# Exe 1
# Example : User input is 15. Add 1 + 5 = 6 will be output

# user_input = input('Please enter two digits number: ')
# # User input is a string so change the data type then add them together.
# print(f'Total of your two digits is: {int(user_input[0]) + int(user_input[1])}')
# -------------------------------------------------------------------------------------
# Exe 2
# BMI Calculator: Equation is BMI = Weight / height * height

# weight and height are float data type they have decimals.
# weight = float(input('What is your weight?: '))
# height = float(input('What is your height?: '))
# bmi = round(weight / (height * height))
# print(f'Your BMI is: {bmi}')
# -------------------------------------------------------------------------------------
# Exe 3
# Create a program using maths and f-Strings that tells us how many days, weeks, months we have left if we live until
# 90 years old.

# current_age = int(input('What is your current age?: '))
# max_age = 90
# left_age = 90 - current_age
# days = 365 * left_age
# weeks = 55 * left_age
# months = 12 * left_age
#
# print(f'You have left: {days}days, {weeks}weeks and {months}months')
# -------------------------------------------------------------------------------------

